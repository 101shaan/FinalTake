{
  "name": "app-root",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "cd frontend && yarn start",
    "build": "cd frontend && yarn build",
    "test": "cd frontend && yarn test"
  }
}